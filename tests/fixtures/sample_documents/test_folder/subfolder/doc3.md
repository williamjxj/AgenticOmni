# Document 3 (Nested)

This document is in a subfolder to test recursive directory traversal.

## Nested Content

This tests that the folder service correctly discovers markdown files in nested subdirectories.

### Path Preservation

The relative path should be preserved: `subfolder/doc3.md`
