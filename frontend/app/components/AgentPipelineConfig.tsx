import React from 'react';

export default function AgentPipelineConfig() {
  // Placeholder UI for configuring agent pipeline
  return (
    <div>
      <h2>Agent Pipeline Configuration</h2>
      <p>Configure the document processing pipeline here.</p>
      {/* Add forms/fields for agent selection and order */}
    </div>
  );
}
